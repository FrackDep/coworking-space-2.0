/// <reference types="expo/types" />

// Tipos globales del entorno Expo/Metro (habilita el tipado de `require()`
// para las imágenes locales de assets/). Se versiona en el repo para que
// `pnpm exec tsc --noEmit` funcione sin necesidad de arrancar Expo primero.
